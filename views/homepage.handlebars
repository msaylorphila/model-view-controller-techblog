{{#each blogPosts as |blogPost|}}
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 id="singleBlogPost" class="card-title">{{blogPost.title}}</h5>
    <div class="card-title">Created by: {{blogPost.username}} on: {{ format_date blogPost.createdAt}}</div>
    <p class="card-text">{{blogPost.contents}}</p>
  </div>
</div>
{{/each}}

<script src="./js/singleblogpost.js"></script>

